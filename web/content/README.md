Markdown for the site, parsed once at startup.

- `posts/` — the devlog. `YYYY-MM-DD-slug.md` with a `+++` TOML frontmatter block.
- `pages/` — standing pages that are prose rather than markup.

Empty until W2. See `lightcone/docs/14-hosting.md`.
